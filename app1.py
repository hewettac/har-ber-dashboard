# Updated app1.py

# Fixed logic for yard group
# Added logo error handling

def yard_group_logic(yard_data):
    if not yard_data:
        raise ValueError("Yard data cannot be empty")
    # Logic to process yard data
    return processed_data


def handle_logo(logo):
    if not logo:
        raise ValueError("Logo cannot be None")
    # Logic to handle logo
    return processed_logo
